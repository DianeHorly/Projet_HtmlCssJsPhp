CREATE TABLE tab_contatti(
    id int AUTO_INCREMENT PRIMARY KEY,
	nome varchar(50) NOT NULL,
	cognome varchar(50) NOT NULL,
    sesso varchar(10) NOT NULL,
	email varchar(20) NOT NULL,
	commento varchar(100) NOT NULL,
);
	
